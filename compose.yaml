services:
  server:
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
      - ./credentials:/app/credentials
      - ./tracker.db:/app/tracker.db

  frontend:
    build:
      context: ./frontend
    ports:
      - 5173:5173